# Ville -Ideale scrapper

## How to run

using yarn is recommended, but npm can be used too

```shell
# In the root reporsitory folder :  
yarn install
mkdir out
yarn start
```

The json files will be stored in the "out" folder.